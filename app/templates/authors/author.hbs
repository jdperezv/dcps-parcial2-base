
<div class="author-detail">
    <p><a>First name:</a>
    <a>{{model.firstName}}</a>
    </p>
    <p> <a>Last name:</a>
        <a>
        {{model.lastName}}</a></p>
{{#each model.blogPost as |post|}}
<a>{{post.title}}</a>
<a>{{post.publishedDate}}</a>
<a>{{post.content}}</a>
<button value="Eliminar Post">Eliminar Post</button>
{{/each}}
</div>
{{outlet}}